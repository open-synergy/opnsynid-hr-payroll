Adds a salary summary report for Employee Payslip Batch.

The report shows all payslips in a batch as rows and salary rules as dynamic columns,
ordered by salary rule sequence. The report can be viewed on-screen as HTML (with
links to individual payslips), exported to XLSX, or printed as PDF.
